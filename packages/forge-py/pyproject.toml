[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "forge-sdk"
version = "0.1.0"
description = "Forge Python bindings"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
